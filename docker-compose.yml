services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - TEMP_DIR=/app/tmp
      - MAX_UPLOAD_BYTES=10485760
      - RATE_LIMIT_PER_MINUTE=30
    volumes:
      - .:/app
      - /app/node_modules
